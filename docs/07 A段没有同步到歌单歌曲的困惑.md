CPU 大人，我发现了——`syncFolderPlaylist` 是整个代码库里**没有任何调用者的死代码**！它定义在 `PlaylistRepository.kt:57`，但 `MainViewModel` 和 `PlaylistViewModel` 都未调用它。

实际创建歌单时走的是 `importFoldersIndividually`（`PlaylistViewModel.kt:87`），它直接用内存中已构建好的 `folder.songs.map { it.id }` 一次性加进去。而这些 `folder.songs` 来自 `processFolders`（`LibraryViewModel.kt:120`），用的是 `allSongs` StateFlow，后者查询 `WHERE IsAbPartB = 0`。A 段歌曲的 `isAbPartB = false`，按理应该被包含在内。

您说的 A 段歌曲没同步上去，具体是指哪个场景呢？(´･_･`)

1. **`importFoldersIndividually` 导入歌单时** A 段歌曲缺失？
2. 还是**您希望 `syncFolderPlaylist` 这段死代码被启用**，但发现它对 A 段的处理有问题？
3. 还是**扫描后歌单列表查询**（`getSongsInPlaylistRaw` 带 `IsAbPartB = 0` 过滤）漏掉了 A 段？

CPU 大人，我仔细走了一遍代码，从逻辑上看 A 段歌曲是能通过流程进入歌单的。关键点在于 `PcDatabaseImporter.kt:240-241`：

```kotlin
val refreshedSongsMap =
    songDao.getAllSongs().groupBy { it.fileName.lowercase() }
```

`getAllSongs()` 过滤 `IsAbPartB = 0`，A 段歌曲的 `isAbPartB` 是 `false`，按理应该在其中。

但第225行元数据更新时：
```kotlin
isAbPartB = matchedSong.isAbPartB
```

用的 `matchedSong` 来自第69行的 `localSongsMap`（`getAllSongsRaw()` 不过滤 B 段）。这里有一个隐患：如果手机端 A/B 两首歌的 `totalSamples` 在 PC 匹配时因为容差逻辑导致**A 段歌曲匹配到了手机本地的 B 段歌曲**，那 `isAbPartB` 就会被标记为 `true`！

比如手机端的 A 段 `totalSamples=100000`、B 段 `totalSamples=100050`，PC 端 A 段 `totalSamples=100020` 在容差 `10000` 内，但匹配时 `candidates?.find` 可能先找到了 B 段（采样数更接近），导致 `matchedSong = B 段 (isAbPartB=true)`，然后元数据更新把 A 段的 `isAbPartB` 写成了 `true`。这样 `getAllSongs()` 就会把它过滤掉！

要不我把这里的匹配逻辑加上**文件名排除 B 段**的条件，确保 A 段的元数据更新不会匹配到 B 段？(｀・ω・´)